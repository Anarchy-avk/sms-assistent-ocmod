<?xml version="1.0" encoding="UTF-8"?>
<modification>
	<name>SMS-Assistent Module</name>
	<version>1.1</version>
	<author>SMS-Assistent</author>
	<code>5DaDW2cXHMnUJ1AoFkZxmKa0pJiw0WU5</code>

	<file path="system/startup.php">
        <operation>
            <search><![CDATA[
                // Engine
            ]]></search>
            <add position="after"><![CDATA[
                require_once(DIR_SYSTEM . 'library/smsassistent.php');
            ]]></add>
        </operation>
    </file>

    <file path="catalog/model/checkout/order.php">
        <operation>
            <search><![CDATA[
                // Admin Alert Mail
            ]]></search>
            <add position="after"><![CDATA[
                if ($this->config->get('smsassistent_status') == "1") {
                    if ($this->config->get('smsassistent_customer_order_create_status') == "1") {
                        $sms = new SMSAssistent($this->config);
                        $sms->sendCustomerNotification($order_info, $order_product_query, $this->currency);
                    }
                    if ($this->config->get('smsassistent_admin_order_create_status') == "1") {
                        $sms = new SMSAssistent($this->config);
                        $sms->sendAdminNotification($order_info, $order_product_query, $this->currency);
                    }
                }
            ]]></add>
        </operation>
    </file>
</modification>